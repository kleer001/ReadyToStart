[Audio]
connections = Graphics, User, Network
requires =

[Graphics]
connections = Audio, Display, Performance
requires = Audio

[User]
connections = Security, Privacy, Profile
requires = Audio, Graphics

[Display]
connections = Graphics, Performance, Visual
requires = Graphics

[Performance]
connections = Graphics, Display, System
requires =

[Network]
connections = Security, Privacy, System
requires = Audio

[Security]
connections = User, Network, Privacy
requires = User

[Privacy]
connections = User, Network, Security
requires =

[Profile]
connections = User, Security, Data
requires = User

[Visual]
connections = Display, Graphics, Appearance
requires = Display

[System]
connections = Performance, Network, Hardware
requires = Performance

[Hardware]
connections = System, Performance, Device
requires = System

[Device]
connections = Hardware, Input, Output
requires = Hardware

[Input]
connections = Device, User, Control
requires = Device

[Output]
connections = Device, Display, Audio
requires = Device

[Control]
connections = Input, User, Interface
requires = Input

[Interface]
connections = Control, Visual, User
requires =

[Appearance]
connections = Visual, Interface, Theme
requires = Visual

[Theme]
connections = Appearance, Visual, Color
requires = Appearance

[Color]
connections = Theme, Display, Visual
requires = Theme

[Data]
connections = Profile, Storage, Cache
requires = Profile

[Storage]
connections = Data, System, File
requires = Data

[File]
connections = Storage, System, Access
requires = Storage

[Access]
connections = File, Security, Permission
requires = File

[Permission]
connections = Access, Security, User
requires = Access

[Cache]
connections = Data, Performance, Memory
requires = Data

[Memory]
connections = Cache, Performance, System
requires = Cache
